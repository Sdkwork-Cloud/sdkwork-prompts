//! Generated gateway bootstrap for sdkwork-prompts.
//! Regenerated by pnpm gateway:assembly:materialize when bootstrap contains todo!().

use axum::Router;

pub struct ApplicationAssembly {
    pub router: Router,
}

pub fn assemble_application_router() -> ApplicationAssembly {
    let mut router = Router::new();
    router = router.merge(sdkwork_routes_prompts_app_api::gateway_mount());
    router = router.merge(sdkwork_routes_prompts_backend_api::gateway_mount());
    router = router.merge(sdkwork_routes_prompts_open_api::gateway_mount());
    ApplicationAssembly { router }
}
