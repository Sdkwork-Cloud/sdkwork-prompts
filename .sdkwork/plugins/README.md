# SDKWork Prompts Plugins

Add repository-local agent plugins here only when the plugin is source-controlled and safe to share.

Each plugin must include `.sdkwork/plugins/<plugin-name>/.codex-plugin/plugin.json`.
