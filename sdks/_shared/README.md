# Shared SDK Inputs

Shared SDK generation fixtures and schema fragments may live here when they are not authoritative API contracts.

## Status

No shared schemas currently needed. Each SDK family owns its own schemas independently.

Shared extraction will be added only if duplicate schemas across SDK families become hard to maintain.
